[user]
	name = Willington Vega
	email = wvega@wvega.com
[core]
	autocrlf = input
	excludesfile = ~/.gitignore.global
	filemode = true
[alias]
    ch = log --first-parent --pretty=format:'- %s'
    co = checkout
    goback = reset HEAD~1
    merge-point = "!f() { git log --merges --oneline --color=always $1 | tail -1; }; f"
    merged-with = "!f() { git log --oneline --merges --ancestry-path --color=always $1..$2 | tail -1; }; f"
    mf = diff --name-only
    show-merge-base = "!f() { git show $(git merge-base $1 $2); }; f"
    st = status
    wip = !git add -A && git cm "WIP"
[merge]
	tool = opendiff
